name: CI

on:
  push:

permissions:
  contents: read

jobs:
  test:
    name: Ruby ${{ matrix.ruby-label }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        include:
          - ruby-version: "4.0.5"
            ruby-label: "4.0.5"
          - ruby-version: ruby-head
            ruby-label: "4.1-dev"

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup root bundle
        uses: ruby/setup-ruby@9eb537ca036ebaed86729dcb9309076e4c5c3b74 # v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true

      - name: rbs validate
        run: bundle exec rbs validate

      - name: steep check
        run: bundle exec steep check

      - name: rake test
        run: bundle exec rake test

      - name: rake test (RUBY_BOX=true)
        run: bundle exec rake test
        env:
          RUBY_BOX: "true"

      - name: Setup rails-modular-monolith bundle
        uses: ruby/setup-ruby@9eb537ca036ebaed86729dcb9309076e4c5c3b74 # v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true
          working-directory: example/rails-modular-monolith

      - name: Install rails-modular-monolith module bundles
        run: |
          BUNDLE_GEMFILE=modules/foo/Gemfile bundle install
          BUNDLE_GEMFILE=modules/bar/Gemfile bundle install
        working-directory: example/rails-modular-monolith

      - name: rails-modular-monolith test
        run: bundle exec bin/box-rails test
        working-directory: example/rails-modular-monolith
        env:
          RUBYLIB: ${{ github.workspace }}/lib

      - name: Setup rails-modular-monolith-with-rails-engine bundle
        uses: ruby/setup-ruby@9eb537ca036ebaed86729dcb9309076e4c5c3b74 # v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true
          working-directory: example/rails-modular-monolith-with-rails-engine

      - name: Install rails-modular-monolith-with-rails-engine module bundles
        run: |
          BUNDLE_GEMFILE=modules/foo/Gemfile bundle install
          BUNDLE_GEMFILE=modules/bar/Gemfile bundle install
        working-directory: example/rails-modular-monolith-with-rails-engine

      - name: rails-modular-monolith-with-rails-engine test
        run: bundle exec bin/box-rails test
        working-directory: example/rails-modular-monolith-with-rails-engine
        env:
          RUBYLIB: ${{ github.workspace }}/lib
