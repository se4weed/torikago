<main style="padding: 2rem; font-family: monospace;">
  <h1>/baz/showcase</h1>
  <p>The <code>baz</code> box is only allowed to call the <code>foo</code> product query.</p>

  <pre style="padding: 1rem; background: #111; color: #f6f7fb; border-radius: 12px;"><%= @plain_banner %></pre>
</main>
