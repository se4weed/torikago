#!/usr/bin/env ruby
require "tmpdir"

unless Dir.respond_to?(:tmpdir)
  class << Dir
    def tmpdir
      ENV["TMPDIR"] || "/tmp"
    end
  end
end

require_relative "../config/boot"
require "rake"
Rake.application.run
