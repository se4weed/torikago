<main style="max-width: 720px; margin: 0 auto; padding: 2rem; font-family: monospace;">
  <h1>/qux/showcase</h1>
  <p>This module does not define a Rails::Engine. The host app owns this route, while Torikago runs the controller in the Qux box.</p>
  <p>Current module box: <code class="current-box"><%= @current_box %></code></p>
  <pre><%= @plain_banner %></pre>
</main>
