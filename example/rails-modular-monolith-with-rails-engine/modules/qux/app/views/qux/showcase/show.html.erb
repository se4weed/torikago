<main style="max-width: 720px; margin: 0 auto; padding: 2rem; font-family: monospace;">
  <h1>/qux/showcase</h1>
  <p>This module does not define a Rails::Engine. The host app owns this route, while Torikago owns the Qux box.</p>
  <pre><%= @plain_banner %></pre>
</main>
